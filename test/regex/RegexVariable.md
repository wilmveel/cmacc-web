// variable
$ hello

// variable
$ hello = null

// variable
$ hello = "World"

// object
$ obj = {
    "hello" : "World"
}

// import
$ imp = [./doc.md]

// import
$ imp_over = [./doc.md] => {
    "hello" : "World"
}

// import
$ imp_over_obj = [./doc.md] => {
    "hello" : {
        "hello" : "World"
    }
}

{{obj}}
